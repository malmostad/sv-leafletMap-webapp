(() => {

    'use strict';

    var router = require('router'),
        globalAppData = require('globalAppData'),

        logUtil = require('LogUtil');

    router.get('/', (req, res) => {

        res.render({

        });
    });

})();